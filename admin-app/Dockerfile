FROM node:20-alpine AS build

WORKDIR /app

COPY package*.json ./
RUN npm i

COPY . .

ARG VITE_API_URL
ARG VITE_PORTAL_URL

ENV VITE_API_URL=$VITE_API_URL
ENV VITE_PORTAL_URL=$VITE_PORTAL_URL

RUN npm run build
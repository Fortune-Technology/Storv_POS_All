## Summary

Eight backlog items shipped across six sub-sessions. Each ships with a focused pure-function smoke. Total: **415/415** across all 8 active suites (was 364/364 before C4).

| ID | Item | Smoke |
|---|---|---|
| F19 | Commission report PDF + CSV export | 24 |
| F18 | Camera ticket scan in lottery EoD wizard | 16 (wiring) |
| C3  | Customer display dark/light theme toggle | 22 |
| F27 | Per-pump reports + alphanumeric pump labels | 56/56 |
| F24 | AI conversation export to Markdown (all 3 widgets) | 68/68 |
| C5  | Label preview pixel accuracy (DPI-driven + JsBarcode) | 74/74 |
| C10 | Settlement snapshot-coverage indicator | 36/36 |
| C4  | Cashier-app light theme | 51/51 |

## What's in here

- **F19** — Commission tab (Lottery → Commission) gets ⬇ CSV + ⬇ PDF buttons next to Apply. Uses shared `exportUtils`. Filename includes date range.
- **F18** — Camera button next to keyboard scan in the lottery EoD wizard step 1. Reuses cashier-app `BarcodeScannerModal` (native `BarcodeDetector` API → `@zxing/browser` fallback for iOS Safari).
- **C3** — `customerDisplay.theme: 'dark' | 'light'` in posConfig; POSScreen broadcasts on every cart_update / idle / transaction_complete; CustomerDisplayScreen applies `cds-root--light` modifier overriding CSS vars; Store Settings dark/light toggle in POSSettings.
- **F27** — Schema migration `FuelPump.pumpNumber: Int → String` (idempotent SQL cast preserves existing integer values). New `normalizePumpNumber()` validator regex `[A-Za-z0-9_-]{1,16}` coerces legacy integer callers + trims whitespace. `getFuelReport` extended with `byPump` aggregation alongside existing `byType` + `unattributedCount` in totals. Cashier-app unchanged — display-only callers handle strings transparently.
- **F24** — Download button added to all 3 AI Assistant widgets (portal / admin-app / cashier-app) — exports current conversation as a `.md` file with title + per-message headings + content + tool calls + ticket refs + feedback. Markdown-by-default since Claude already emits markdown.
- **C5** — Replaced hardcoded `scale = 3` with DPI-driven math (`widthDots × scale` where `scale = px-per-dot`). Replaced fake `j % 3` barcode stripes with real `JsBarcode` SVG (UPC-A / EAN-13 / EAN-8 / CODE128 dispatched by data shape). Added 7-step zoom toolbar with 100% "actual size" 1:1 dot mapping. Editor UX deliberately untouched per user spec.
- **C10** — Settlement engine returns `snapshotCoverage: { daysWithSnapshots, daysInPeriod }` computed from the existing `lotteryScanEvent` query (zero extra DB round-trip). 3-tone chip on each settlement card distinguishes a quiet week from a missed-EoD-wizard week (green = full coverage, amber = partial / POS fallback, red = zero snapshots).
- **C4** — Discovery: 95% of the light-theme infrastructure was already in place (`THEMES.dark` + `THEMES.light` × 25 CSS vars each, `applyBranding()` mutating `:root` + `data-pos-theme` attribute, `useBranding()` hook, portal Dark/Light toggle UI). The work was finding + fixing **hardcoded color bypasses** that don't respect the CSS-var cascade — App.css boot screen, ActionBar.css manager-button + scrollbar + action/hold hover, NumpadModal cancel-hover, QuickButtonRenderer back-button. Modals like BarcodeScanner / FuelModal / ManagerPin intentionally stay dark for visual focus / camera UX.

## Schema changes

- `backend/prisma/migrations/manual_s79b_fuel_pump_number_to_text.sql` — idempotent SQL cast `FuelPump.pumpNumber: Int → String`. Existing integer values cleanly coerce. Run via `prisma db execute`.
- No other schema changes.

## Build verification

- Backend `tsc --noEmit` filtered to touched files: ✓ zero errors
- Cashier-app `vite build` ✓ 4.89s clean (PWA generated)
- Portal `vite build` ✓ clean
- Admin-app `vite build` ✓ clean

## Test plan

- [ ] **F19**: Open Lottery → Commission → set a date range → click ⬇ CSV. Verify filename + 4-column body + TOTAL row.
- [ ] **F19 (PDF)**: Same flow, click ⬇ PDF. Verify 3 KPI summary cards (Total Commission / Total Sales / Avg Rate) + by-game table with TOTAL row.
- [ ] **F18**: Open lottery EoD wizard step 1 on a station with active books → click camera icon → verify camera prompts → scan a real lottery ticket → verify the wizard auto-fills Today input on the matching row.
- [ ] **C3**: Portal POSSettings → Customer Display Theme → pick Light → save. Verify second screen flips immediately (not after refresh).
- [ ] **F27**: Portal Fuel → Pumps → add new pump with label "A1" or "Diesel-1" → verify accepts. Try `"A 1"` (space) → verifies rejects.
- [ ] **F27 reports**: Run a few fuel sales attributed to Pump 1 + Pump 2. Open Fuel → Reports tab → verify "By Pump" panel with correct net gallons + amount per pump.
- [ ] **F24**: Open AI Assistant on any of the 3 apps → send a few messages → click Download icon → verify `.md` file downloads with correct content.
- [ ] **C5**: Portal Label Design → load any template → verify preview pane size matches actual label dimensions at the printer's DPI. Try barcode field with 12-digit UPC → verify renders as UPC-A. Try 100% zoom → verify 1:1 dot mapping.
- [ ] **C10**: Lottery → Weekly Settlement → verify per-row snapshot coverage chip (green/amber/red) and tooltip explanation.
- [ ] **C4**: Portal Store Branding → flip theme to Light. Verify cashier-app POS chrome flips on next config-poll (or refresh). Specifically verify ActionBar manager button + scrollbar + action hover are visible (not transparent).

## Smoke regression — 415/415

```
_smoke_c9_cash_drawer_events:        32/32
_smoke_s78_implementation_pin:       36/36
_smoke_s79_three_items:              62/62
_smoke_s79b_f27_per_pump:            56/56
_smoke_s79c_f24_conversation_export: 68/68
_smoke_s79d_c5_label_preview_math:   74/74
_smoke_s79e_c10_snapshot_coverage:   36/36
_smoke_s79f_c4_cashier_light_theme:  51/51
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

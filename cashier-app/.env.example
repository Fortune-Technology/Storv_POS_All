# ─── Cashier App (POS Terminal) Environment Configuration ───────────────────
# Must point to your backend API's /api endpoint.

# Development (default — Vite proxy forwards /api to localhost:5000)
VITE_API_URL=http://localhost:5000/api

# Production example:
# VITE_API_URL=https://api-pos.thefortunetech.com/api

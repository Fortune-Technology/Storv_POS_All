# ─── Cashier App — Production Environment ──────────────────────────────────
# This URL is baked into the Vite bundle at build time.
# For Electron desktop builds, the runtime override in client.js takes precedence.
VITE_API_URL=https://pos.storeveu.com/api

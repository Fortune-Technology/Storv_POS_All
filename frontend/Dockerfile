FROM node:20-alpine AS build

WORKDIR /app

COPY package*.json ./
RUN npm i

COPY . .

ARG VITE_API_URL
ARG VITE_ECOM_URL
ARG VITE_STOREFRONT_URL

ENV VITE_API_URL=$VITE_API_URL
ENV VITE_ECOM_URL=$VITE_ECOM_URL
ENV VITE_STOREFRONT_URL=$VITE_STOREFRONT_URL

RUN npm run build
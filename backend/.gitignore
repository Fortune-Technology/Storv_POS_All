node_modules/
uploads/
.env
*.log
coverage/
.DS_Store
prisma/.seed-credentials
.seed-credentials
# Tax-rule migration reports — per-org CSVs emitted by migrateTaxRules.js
prisma/.migrations/
# package-lock.json must be committed so CI can run `npm ci`
# Do NOT add package-lock.json here

package-lock.json

# TypeScript incremental build cache — regenerated on every `tsc --noEmit`
tsconfig.tsbuildinfo